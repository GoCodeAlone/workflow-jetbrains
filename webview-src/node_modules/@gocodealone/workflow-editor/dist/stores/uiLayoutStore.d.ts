interface PanelWidths {
    projectSwitcher: number;
    nodePalette: number;
    propertyPanel: number;
}
interface UILayoutStore {
    projectSwitcherCollapsed: boolean;
    nodePaletteCollapsed: boolean;
    propertyPanelCollapsed: boolean;
    panelWidths: PanelWidths;
    toggleProjectSwitcher: () => void;
    toggleNodePalette: () => void;
    togglePropertyPanel: () => void;
    setProjectSwitcherCollapsed: (collapsed: boolean) => void;
    setNodePaletteCollapsed: (collapsed: boolean) => void;
    setPropertyPanelCollapsed: (collapsed: boolean) => void;
    setPanelWidth: (panel: keyof PanelWidths, width: number) => void;
}
declare const PANEL_WIDTH_LIMITS: Record<keyof PanelWidths, {
    min: number;
    max: number;
}>;
export { PANEL_WIDTH_LIMITS };
declare const useUILayoutStore: import("zustand").UseBoundStore<Omit<import("zustand").StoreApi<UILayoutStore>, "setState" | "persist"> & {
    setState(partial: UILayoutStore | Partial<UILayoutStore> | ((state: UILayoutStore) => UILayoutStore | Partial<UILayoutStore>), replace?: false | undefined): unknown;
    setState(state: UILayoutStore | ((state: UILayoutStore) => UILayoutStore), replace: true): unknown;
    persist: {
        setOptions: (options: Partial<import("zustand/middleware").PersistOptions<UILayoutStore, UILayoutStore, unknown>>) => void;
        clearStorage: () => void;
        rehydrate: () => Promise<void> | void;
        hasHydrated: () => boolean;
        onHydrate: (fn: (state: UILayoutStore) => void) => () => void;
        onFinishHydration: (fn: (state: UILayoutStore) => void) => () => void;
        getOptions: () => Partial<import("zustand/middleware").PersistOptions<UILayoutStore, UILayoutStore, unknown>>;
    };
}>;
export default useUILayoutStore;
