import { create as k } from "zustand";
import { persist as P } from "zustand/middleware";
import { addEdge as E, applyEdgeChanges as x, applyNodeChanges as M } from "@xyflow/react";
import { MODULE_TYPE_MAP as O, MODULE_TYPES as h } from "./types/index.js";
import { l as W, a as L, c as j, n as F, b as H, i as _ } from "./connectionCompatibility-BFMaaxPl.js";
function $(n) {
  switch (n) {
    case "string":
    case "duration":
      return "string";
    case "number":
      return "number";
    case "boolean":
      return "boolean";
    case "select":
      return "select";
    case "array":
      return "array";
    case "map":
      return "map";
    case "json":
      return "json";
    case "filepath":
      return "filepath";
    case "sql":
      return "sql";
    default:
      return "string";
  }
}
function C(n) {
  return n.map((o) => ({
    key: o.key,
    label: o.label,
    type: $(o.type),
    options: o.options,
    defaultValue: o.defaultValue,
    description: o.description,
    placeholder: o.placeholder,
    required: o.required,
    group: o.group,
    arrayItemType: o.arrayItemType,
    mapValueType: o.mapValueType,
    inheritFrom: o.inheritFrom,
    sensitive: o.sensitive
  }));
}
function I(n, o) {
  const e = n ?? [], d = o ?? [];
  if (!(e.length === 0 && d.length === 0))
    return {
      inputs: e.map((t) => ({ name: t.name, type: t.type })),
      outputs: d.map((t) => ({ name: t.name, type: t.type }))
    };
}
const A = [
  "http",
  "messaging",
  "statemachine",
  "events",
  "integration",
  "scheduling",
  "infrastructure",
  "middleware",
  "database",
  "observability",
  "pipeline",
  "cicd",
  "security",
  "deployment"
];
function S(n) {
  return A.includes(n) ? n : "infrastructure";
}
function v(n, o) {
  const e = [], d = /* @__PURE__ */ new Set();
  for (const t of n) {
    d.add(t.type);
    const a = o[t.type];
    if (a) {
      const s = I(a.inputs, a.outputs);
      e.push({
        ...t,
        label: a.label || t.label,
        category: S(a.category || t.category),
        configFields: a.configFields.length > 0 ? C(a.configFields) : t.configFields,
        defaultConfig: a.defaultConfig ?? t.defaultConfig,
        ioSignature: s ?? t.ioSignature,
        maxIncoming: a.maxIncoming ?? t.maxIncoming,
        maxOutgoing: a.maxOutgoing ?? t.maxOutgoing
      });
    } else
      e.push(t);
  }
  for (const [t, a] of Object.entries(o))
    d.has(t) || e.push({
      type: t,
      label: a.label,
      category: S(a.category),
      configFields: C(a.configFields),
      defaultConfig: a.defaultConfig ?? {},
      ioSignature: I(a.inputs, a.outputs),
      maxIncoming: a.maxIncoming,
      maxOutgoing: a.maxOutgoing
    });
  return e;
}
function J(n, o, e) {
  return {
    type: n,
    label: o.label ?? n,
    category: S(o.category ?? "integration"),
    configFields: o.configFields ?? [],
    defaultConfig: o.defaultConfig ?? {},
    ioSignature: o.ioSignature,
    maxIncoming: o.maxIncoming,
    maxOutgoing: o.maxOutgoing
  };
}
const f = k((n, o) => ({
  loaded: !1,
  loading: !1,
  serverSchemas: {},
  moduleTypes: h,
  moduleTypeMap: O,
  services: [],
  servicesLoaded: !1,
  fetchSchemas: async () => {
    if (!o().loading) {
      n({ loading: !0 });
      try {
        const e = localStorage.getItem("auth_token"), d = {};
        e && (d.Authorization = `Bearer ${e}`);
        const t = await fetch("/api/v1/admin/schemas/modules", { headers: d });
        if (!t.ok) {
          console.warn("Failed to fetch module schemas, using static defaults"), n({ loading: !1, loaded: !0 });
          return;
        }
        const a = await t.json(), s = v(h, a), l = Object.fromEntries(s.map((r) => [r.type, r]));
        n({
          serverSchemas: a,
          moduleTypes: s,
          moduleTypeMap: l,
          loaded: !0,
          loading: !1
        });
      } catch (e) {
        console.warn("Error fetching module schemas:", e), n({ loading: !1, loaded: !0 });
      }
    }
  },
  fetchServices: async () => {
    try {
      const e = localStorage.getItem("auth_token"), d = {};
      e && (d.Authorization = `Bearer ${e}`);
      let t = await fetch("/api/v1/admin/engine/services", { headers: d });
      if (t.ok || (t = await fetch("/api/workflow/services", { headers: d })), !t.ok) {
        console.warn("Failed to fetch services"), n({ servicesLoaded: !0 });
        return;
      }
      const a = await t.json();
      n({ services: a, servicesLoaded: !0 });
    } catch (e) {
      console.warn("Error fetching services:", e), n({ servicesLoaded: !0 });
    }
  },
  loadSchemas: (e) => {
    const d = v(h, e), t = Object.fromEntries(d.map((a) => [a.type, a]));
    n({
      serverSchemas: e,
      moduleTypes: d,
      moduleTypeMap: t,
      loaded: !0,
      loading: !1
    });
  },
  loadPluginSchemas: (e) => {
    const { moduleTypes: d, moduleTypeMap: t } = o(), a = [...d], s = { ...t };
    for (const l of e)
      for (const [r, u] of Object.entries(l.modules)) {
        const c = J(r, u, l.pluginName);
        if (!s[r])
          a.push(c);
        else {
          const i = a.findIndex((p) => p.type === r);
          i >= 0 && (a[i] = c);
        }
        s[r] = c;
      }
    n({ moduleTypes: a, moduleTypeMap: s });
  }
}));
let D = 0, y = 1;
function N(n, o) {
  return {
    id: n,
    name: o,
    nodes: [],
    edges: [],
    undoStack: [],
    redoStack: [],
    dirty: !1
  };
}
const Y = k()(
  P(
    (n, o) => ({
      nodes: [],
      edges: [],
      selectedNodeId: null,
      selectedEdgeId: null,
      nodeCounter: 0,
      // Preserved workflow/trigger sections from imported config
      importedWorkflows: {},
      importedTriggers: {},
      importedPipelines: {},
      // Active workflow record
      activeWorkflowRecord: null,
      setActiveWorkflowRecord: (e) => n({ activeWorkflowRecord: e }),
      // Tab management
      tabs: [N("default", "Workflow 1")],
      activeTabId: "default",
      crossWorkflowLinks: [],
      // Toast
      toasts: [],
      addToast: (e, d) => {
        const t = `toast-${++D}`;
        n({ toasts: [...o().toasts, { id: t, message: e, type: d }] });
      },
      removeToast: (e) => {
        n({ toasts: o().toasts.filter((d) => d.id !== e) });
      },
      // Undo/redo
      undoStack: [],
      redoStack: [],
      pushHistory: () => {
        const { nodes: e, edges: d, undoStack: t } = o(), a = {
          nodes: JSON.parse(JSON.stringify(e)),
          edges: JSON.parse(JSON.stringify(d))
        };
        n({
          undoStack: [...t.slice(-49), a],
          redoStack: []
        });
      },
      undo: () => {
        const { undoStack: e, nodes: d, edges: t } = o();
        if (e.length === 0) return;
        const a = e[e.length - 1];
        n({
          undoStack: e.slice(0, -1),
          redoStack: [
            ...o().redoStack,
            { nodes: JSON.parse(JSON.stringify(d)), edges: JSON.parse(JSON.stringify(t)) }
          ],
          nodes: a.nodes,
          edges: a.edges
        });
      },
      redo: () => {
        const { redoStack: e, nodes: d, edges: t } = o();
        if (e.length === 0) return;
        const a = e[e.length - 1];
        n({
          redoStack: e.slice(0, -1),
          undoStack: [
            ...o().undoStack,
            { nodes: JSON.parse(JSON.stringify(d)), edges: JSON.parse(JSON.stringify(t)) }
          ],
          nodes: a.nodes,
          edges: a.edges
        });
      },
      // View level
      viewLevel: "component",
      setViewLevel: (e) => n({ viewLevel: e }),
      // UI panels
      showAIPanel: !1,
      showComponentBrowser: !1,
      toggleAIPanel: () => n({ showAIPanel: !o().showAIPanel }),
      toggleComponentBrowser: () => n({ showComponentBrowser: !o().showComponentBrowser }),
      onNodesChange: (e) => {
        n({ nodes: M(e, o().nodes) });
      },
      onEdgesChange: (e) => {
        n({ edges: x(e, o().edges) });
      },
      onConnect: (e) => {
        o().pushHistory();
        const { source: d, target: t } = e, a = o().nodes, s = a.find((r) => r.id === d), l = a.find((r) => r.id === t);
        if (s && l && _(s.data.moduleType, l.data.moduleType)) {
          const u = o().edges.filter(
            (p) => p.data?.edgeType === "pipeline-flow"
          ).length + 1, i = {
            id: `e-pipeline-flow-${d}-${t}`,
            source: d,
            target: t,
            sourceHandle: e.sourceHandle ?? void 0,
            targetHandle: e.targetHandle ?? void 0,
            data: { edgeType: "pipeline-flow", chainOrder: u }
          };
          n({ edges: [...o().edges, i] });
        } else
          n({ edges: E(e, o().edges) });
      },
      setSelectedNode: (e) => n({ selectedNodeId: e, selectedEdgeId: null }),
      setSelectedEdge: (e) => n({ selectedEdgeId: e, selectedNodeId: null }),
      removeEdge: (e) => {
        o().pushHistory(), n({
          edges: o().edges.filter((d) => d.id !== e),
          selectedEdgeId: o().selectedEdgeId === e ? null : o().selectedEdgeId
        });
      },
      addNode: (e, d) => {
        const a = f.getState().moduleTypeMap[e] ?? O[e];
        if (!a) return;
        o().pushHistory();
        const s = o().nodeCounter + 1, r = {
          id: `${e.replace(/\./g, "_")}_${s}`,
          type: H(e),
          position: d,
          data: {
            moduleType: e,
            label: `${a.label} ${s}`,
            config: { ...a.defaultConfig }
          }
        }, u = o().nodes;
        if (n({
          nodes: [...u, r],
          nodeCounter: s
        }), e.startsWith("http.middleware.")) {
          const c = u.filter(
            (i) => i.data.moduleType === "http.router"
          );
          if (c.length === 1) {
            const i = c[0], p = i.data.config?.middlewareChain ?? [];
            n({
              nodes: o().nodes.map(
                (g) => g.id === i.id ? { ...g, data: { ...g.data, config: { ...g.data.config, middlewareChain: [...p, r.data.label] } } } : g
              )
            });
          } else if (c.length > 1) {
            let i = c[0], p = 1 / 0;
            for (const m of c) {
              const w = m.position.x - d.x, b = m.position.y - d.y, T = w * w + b * b;
              T < p && (p = T, i = m);
            }
            const g = i.data.config?.middlewareChain ?? [];
            n({
              nodes: o().nodes.map(
                (m) => m.id === i.id ? { ...m, data: { ...m.data, config: { ...m.data.config, middlewareChain: [...g, r.data.label] } } } : m
              )
            }), o().addToast(
              `Auto-associated "${r.data.label}" with nearest router "${i.data.label}"`,
              "info"
            );
          }
        }
      },
      removeNode: (e) => {
        o().pushHistory(), n({
          nodes: o().nodes.filter((d) => d.id !== e),
          edges: o().edges.filter((d) => d.source !== e && d.target !== e),
          selectedNodeId: o().selectedNodeId === e ? null : o().selectedNodeId
        });
      },
      updateNodeConfig: (e, d) => {
        o().pushHistory(), n({
          nodes: o().nodes.map(
            (t) => t.id === e ? { ...t, data: { ...t.data, config: { ...t.data.config, ...d } } } : t
          )
        });
      },
      updateNodeName: (e, d) => {
        o().pushHistory(), n({
          nodes: o().nodes.map(
            (t) => t.id === e ? { ...t, data: { ...t.data, label: d } } : t
          )
        });
      },
      updateHandlerRoutes: (e, d) => {
        o().pushHistory(), n({
          nodes: o().nodes.map(
            (t) => t.id === e ? { ...t, data: { ...t.data, handlerRoutes: d } } : t
          )
        });
      },
      exportToConfig: () => {
        const { nodes: e, edges: d, importedWorkflows: t, importedTriggers: a, importedPipelines: s } = o(), l = f.getState().moduleTypeMap, r = F(e, d, l);
        return Object.keys(r.workflows).length === 0 && Object.keys(t).length > 0 && (r.workflows = t), Object.keys(r.triggers).length === 0 && Object.keys(a).length > 0 && (r.triggers = a), Object.keys(s).length > 0 && (r.pipelines = s), r;
      },
      importFromConfig: (e) => {
        o().pushHistory();
        const d = f.getState().moduleTypeMap, { nodes: t, edges: a } = j(e, d);
        n({
          nodes: t,
          edges: a,
          selectedNodeId: null,
          importedWorkflows: e.workflows ?? {},
          importedTriggers: e.triggers ?? {},
          importedPipelines: e.pipelines ?? {}
        });
      },
      clearCanvas: () => {
        o().pushHistory(), n({ nodes: [], edges: [], selectedNodeId: null, selectedEdgeId: null, nodeCounter: 0, importedWorkflows: {}, importedTriggers: {}, importedPipelines: {} });
      },
      // Validation errors (transient, not persisted)
      validationErrors: [],
      setValidationErrors: (e) => n({ validationErrors: e }),
      clearValidationErrors: () => n({ validationErrors: [] }),
      // Connection drag state
      connectingFrom: null,
      compatibleNodeIds: [],
      connectionPicklist: null,
      setConnectingFrom: (e) => n({ connectingFrom: e }),
      setCompatibleNodeIds: (e) => n({ compatibleNodeIds: e }),
      showConnectionPicklist: (e) => n({ connectionPicklist: e }),
      hideConnectionPicklist: () => n({ connectionPicklist: null, connectingFrom: null, compatibleNodeIds: [] }),
      // Snap-to-connect state
      snapTargetId: null,
      setSnapTargetId: (e) => n({ snapTargetId: e }),
      // Tab actions
      addTab: () => {
        const { tabs: e, nodes: d, edges: t, undoStack: a, redoStack: s, activeTabId: l } = o();
        y++;
        const r = `tab-${Date.now()}`, u = N(r, `Workflow ${y}`), c = e.map(
          (i) => i.id === l ? {
            ...i,
            nodes: structuredClone(d),
            edges: structuredClone(t),
            undoStack: structuredClone(a),
            redoStack: structuredClone(s)
          } : i
        );
        n({
          tabs: [...c, u],
          activeTabId: r,
          nodes: [],
          edges: [],
          undoStack: [],
          redoStack: [],
          selectedNodeId: null,
          selectedEdgeId: null,
          nodeCounter: 0
        });
      },
      closeTab: (e) => {
        const { tabs: d, activeTabId: t } = o();
        if (d.length <= 1) return;
        const a = d.findIndex((l) => l.id === e), s = d.filter((l) => l.id !== e);
        if (e === t) {
          const l = s[Math.min(a, s.length - 1)];
          n({
            tabs: s,
            activeTabId: l.id,
            nodes: l.nodes,
            edges: l.edges,
            undoStack: l.undoStack,
            redoStack: l.redoStack,
            selectedNodeId: null,
            selectedEdgeId: null
          });
        } else
          n({ tabs: s });
      },
      switchTab: (e) => {
        const { activeTabId: d, tabs: t, nodes: a, edges: s, undoStack: l, redoStack: r } = o();
        if (e === d) return;
        const u = t.map(
          (i) => i.id === d ? {
            ...i,
            nodes: structuredClone(a),
            edges: structuredClone(s),
            undoStack: structuredClone(l),
            redoStack: structuredClone(r)
          } : i
        ), c = u.find((i) => i.id === e);
        c && n({
          tabs: u,
          activeTabId: e,
          nodes: c.nodes,
          edges: c.edges,
          undoStack: c.undoStack,
          redoStack: c.redoStack,
          selectedNodeId: null,
          selectedEdgeId: null
        });
      },
      renameTab: (e, d) => {
        n({
          tabs: o().tabs.map((t) => t.id === e ? { ...t, name: d } : t)
        });
      },
      duplicateTab: (e) => {
        const { tabs: d, nodes: t, edges: a, undoStack: s, redoStack: l, activeTabId: r } = o(), u = d.map(
          (g) => g.id === r ? {
            ...g,
            nodes: structuredClone(t),
            edges: structuredClone(a),
            undoStack: structuredClone(s),
            redoStack: structuredClone(l)
          } : g
        ), c = u.find((g) => g.id === e);
        if (!c) return;
        y++;
        const i = `tab-${Date.now()}`, p = {
          ...structuredClone(c),
          id: i,
          name: `Copy of ${c.name}`
        };
        n({
          tabs: [...u, p],
          activeTabId: i,
          nodes: p.nodes,
          edges: p.edges,
          undoStack: p.undoStack,
          redoStack: p.redoStack,
          selectedNodeId: null
        });
      },
      autoGroupOrphans: () => {
        const { nodes: e, edges: d } = o();
        o().pushHistory();
        const t = f.getState().moduleTypeMap, a = L(e, d, t);
        n({ nodes: a.nodes, edges: a.edges });
      },
      autoLayout: () => {
        const { nodes: e, edges: d } = o();
        if (e.length === 0) return;
        o().pushHistory();
        const t = W(e, d);
        n({ nodes: t });
      },
      addCrossWorkflowLink: (e) => {
        n({ crossWorkflowLinks: [...o().crossWorkflowLinks, e] });
      },
      removeCrossWorkflowLink: (e) => {
        n({
          crossWorkflowLinks: o().crossWorkflowLinks.filter((d) => d.id !== e)
        });
      }
    }),
    {
      name: "workflow-store",
      partialize: (n) => {
        const o = n.tabs.map(
          ({ undoStack: t, redoStack: a, ...s }) => s
        ), e = {
          nodes: n.nodes,
          edges: n.edges,
          nodeCounter: n.nodeCounter,
          tabs: o,
          activeTabId: n.activeTabId
        };
        return JSON.stringify(e).length > 4 * 1024 * 1024 ? {
          nodes: [],
          edges: [],
          nodeCounter: n.nodeCounter,
          tabs: o.map(({ nodes: t, edges: a, ...s }) => ({
            ...s,
            nodes: [],
            edges: []
          })),
          activeTabId: n.activeTabId
        } : e;
      }
    }
  )
), V = {
  projectSwitcher: 200,
  nodePalette: 240,
  propertyPanel: 280
}, z = {
  projectSwitcher: { min: 150, max: 350 },
  nodePalette: { min: 180, max: 400 },
  propertyPanel: { min: 200, max: 500 }
}, K = k()(
  P(
    (n, o) => ({
      projectSwitcherCollapsed: !1,
      nodePaletteCollapsed: !1,
      propertyPanelCollapsed: !1,
      panelWidths: { ...V },
      toggleProjectSwitcher: () => n({ projectSwitcherCollapsed: !o().projectSwitcherCollapsed }),
      toggleNodePalette: () => n({ nodePaletteCollapsed: !o().nodePaletteCollapsed }),
      togglePropertyPanel: () => n({ propertyPanelCollapsed: !o().propertyPanelCollapsed }),
      setProjectSwitcherCollapsed: (e) => n({ projectSwitcherCollapsed: e }),
      setNodePaletteCollapsed: (e) => n({ nodePaletteCollapsed: e }),
      setPropertyPanelCollapsed: (e) => n({ propertyPanelCollapsed: e }),
      setPanelWidth: (e, d) => {
        const t = z[e], a = Math.round(Math.max(t.min, Math.min(t.max, d)));
        n({
          panelWidths: { ...o().panelWidths, [e]: a }
        });
      }
    }),
    {
      name: "workflow-ui-layout"
    }
  )
);
export {
  z as P,
  K as a,
  Y as b,
  f as u
};
