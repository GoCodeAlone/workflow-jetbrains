import type { Edge } from '@xyflow/react';
import type { WorkflowNode } from '../stores/workflowStore.ts';
import type { ModuleTypeInfo } from '../types/workflow.ts';
export declare const SNAP_THRESHOLD = 50;
export declare const PUZZLE_OVERLAP = 8;
export interface SnapCandidate {
    targetNodeId: string;
    snappedPosition: {
        x: number;
        y: number;
    };
    sourceNodeId: string;
    targetForEdge: string;
}
/**
 * Find the best snap candidate for a dragged node.
 * Returns the closest compatible node within SNAP_THRESHOLD, or null.
 */
export declare function findSnapCandidate(draggedNodeId: string, draggedPos: {
    x: number;
    y: number;
}, allNodes: WorkflowNode[], edges: Edge[], moduleTypeMap: Record<string, ModuleTypeInfo>, zoom: number): SnapCandidate | null;
