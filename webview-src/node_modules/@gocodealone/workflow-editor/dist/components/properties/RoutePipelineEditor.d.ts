interface PipelineStep {
    name: string;
    type: string;
    config?: Record<string, unknown>;
}
interface RoutePipelineEditorProps {
    steps: PipelineStep[];
    onChange: (steps: PipelineStep[]) => void;
}
export default function RoutePipelineEditor({ steps, onChange }: RoutePipelineEditorProps): import("react/jsx-runtime").JSX.Element;
export {};
