interface FilePickerProps {
    value: string;
    onChange: (path: string) => void;
    projectId?: string;
    placeholder?: string;
    description?: string;
}
export default function FilePicker({ value, onChange, projectId, placeholder, description }: FilePickerProps): import("react/jsx-runtime").JSX.Element;
export {};
