import type { Node } from '@xyflow/react';
interface DelegateServicePickerProps {
    value: string;
    onChange: (value: string) => void;
    placeholder?: string;
    nodes: Node[];
    currentNodeId: string;
}
export default function DelegateServicePicker({ value, onChange, placeholder, nodes, currentNodeId, }: DelegateServicePickerProps): import("react/jsx-runtime").JSX.Element;
export {};
