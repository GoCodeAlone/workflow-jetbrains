import { type ReactNode } from 'react';
interface BaseNodeProps {
    id: string;
    label: string;
    moduleType: string;
    icon: ReactNode;
    preview?: string;
    hasInput?: boolean;
    hasOutput?: boolean;
    children?: ReactNode;
}
export default function BaseNode({ id, label, moduleType, icon, preview, hasInput: hasInputProp, hasOutput: hasOutputProp, children, }: BaseNodeProps): import("react/jsx-runtime").JSX.Element;
export {};
